/**
 * BarbellDiagnostic — placeholder.
 *
 * Registered in src/app/writing/[slug]/page.tsx (since 0bf372c) so a future
 * essay can drop <BarbellDiagnostic /> inline. The real diagnostic figure
 * isn't built yet; this stub returns null so the import resolves at build
 * time without bringing the Vercel deploy down. Replace with the actual
 * component when ready — the registry entry stays the same.
 */
export default function BarbellDiagnostic() {
  return null;
}
